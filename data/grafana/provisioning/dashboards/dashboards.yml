apiVersion: 1

providers:
  - name: 'Default'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10 # Vérifie les mises à jour des tableaux de bord toutes les 10 secondes
    options:
      path: /etc/grafana/provisioning/dashboards
